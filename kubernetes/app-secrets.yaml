kind: ConfigMap
apiVersion: v1
metadata:
  #Note: This name will be used to reference values for the deployment spec
  name: $DOCKUP_APP_NAME-secrets
  namespace: $DOCKUP_KUBERNETES_NAMESPACE
data:
  $DOCKUP_APP_SECRET_STRING_DATA